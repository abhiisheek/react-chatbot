import React from 'react';
import Loader from 'react-chatbot/Loader';

import styles from './CustomLoader.css';

/** Custom loader icon */
const CustomLoader = () => <Loader className={styles.loader}/>;

export default CustomLoader;
